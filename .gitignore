# binários locais (o do app e os comandos de teste de cmd/)
/acessos
/initest
/rdpview
/sftptest
/sshtest
/sshview
/vnctest
/vncview
# artefatos de empacotamento
/build/
/.flatpak-builder/
# vendor é gerado pelo build.sh (go mod vendor) antes de empacotar: ele
# existe para o build do Flatpak rodar OFFLINE, não para versionar 31 MB
# de código de terceiros.
/vendor/
